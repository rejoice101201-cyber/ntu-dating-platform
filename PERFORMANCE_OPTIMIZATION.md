# 效能優化指南

本文件說明咖啡廳探索應用的效能監控和優化策略。

## 效能監控系統

### 後端監控

後端效能監控中間件 (`backend/src/middleware/performance.ts`) 提供以下功能：

- **請求計數**：追蹤總請求數
- **響應時間**：平均、最小、最大響應時間
- **錯誤追蹤**：HTTP 錯誤計數
- **Google Maps API 專用監控**：專門追蹤地圖相關請求
- **記憶體使用**：監控服務器記憶體使用情況

#### 監控端點

- `GET /api/performance` - 獲取效能指標
- `POST /api/performance/reset` - 重置效能指標

### 前端監控

前端效能監控 (`cafe-explorer-frontend/src/utils/performanceMonitor.ts`) 提供：

- **請求快取**：智能快取機制
- **響應時間追蹤**：前端請求效能
- **快取命中率**：快取效率統計
- **Google Maps 請求監控**：地圖 API 效能

## 效能優化策略

### 1. 請求快取

#### 快取配置

```typescript
const CACHE_TTL = {
  PLACES_SEARCH: 300000,    // 5 分鐘
  NEARBY_SEARCH: 600000,    // 10 分鐘
  LOCATIONS: 60000,         // 1 分鐘
  DEFAULT: 300000           // 5 分鐘
};
```

#### 快取策略

- **地點搜尋**：5 分鐘快取，避免重複搜尋
- **附近搜尋**：10 分鐘快取，地理位置變化較慢
- **咖啡廳列表**：1 分鐘快取，平衡即時性和效能
- **自動清理**：過期快取自動清理

### 2. 防抖動機制

#### 搜尋防抖動

```typescript
// 地點搜尋防抖動 (300ms)
const debouncedSearchPlaces = debounce(searchPlaces, 300);

// 附近搜尋防抖動 (500ms)
const debouncedSearchNearby = debounce(searchNearby, 500);
```

#### 防抖動優勢

- **減少 API 請求**：避免快速連續請求
- **提升用戶體驗**：減少不必要的載入狀態
- **節省資源**：降低服務器負載

### 3. Google Maps API 優化

#### 請求優化

- **批量請求**：合併多個地圖操作
- **請求去重**：避免重複的地圖請求
- **錯誤重試**：智能重試機制
- **請求取消**：取消過期的請求

#### 地圖載入優化

```typescript
// 異步載入 Google Maps API
const loadGoogleMaps = (): Promise<void> => {
  // 確保只載入一次
  if (isLoaded && window.google && window.google.maps) {
    return Promise.resolve();
  }
  // 異步載入腳本
  // ...
};
```

### 4. 資料庫優化

#### 查詢優化

- **索引優化**：為常用查詢字段添加索引
- **分頁查詢**：避免一次性載入大量資料
- **查詢快取**：快取常用查詢結果

#### 連線池

```typescript
// SQLite 連線優化
const db = new sqlite3.Database('./db.sqlite', {
  busyTimeout: 5000,
  cache: true
});
```

## 效能監控儀表板

### 功能特色

- **即時監控**：實時顯示效能指標
- **自動刷新**：可選的自動刷新功能
- **效能警告**：慢請求自動警告
- **快取統計**：快取命中率統計
- **記憶體監控**：記憶體使用情況

### 使用方式

1. **開啟儀表板**：點擊右下角 "📊 Performance" 按鈕
2. **查看指標**：實時查看各項效能指標
3. **重置數據**：點擊 "Reset" 按鈕重置統計
4. **自動刷新**：開啟 "Auto" 模式自動刷新

### 指標說明

#### 請求統計
- **Total Requests**：總請求數
- **Errors**：錯誤數量
- **Average Response Time**：平均響應時間
- **Min/Max Response Time**：最小/最大響應時間

#### Google Maps API
- **Requests**：地圖 API 請求數
- **Avg Time**：平均響應時間

#### 快取效能
- **Hit Rate**：快取命中率
- **Cache Size**：快取大小

## 效能最佳實踐

### 1. 前端優化

#### 組件優化
- **React.memo**：避免不必要的重新渲染
- **useCallback**：優化回調函數
- **useMemo**：快取計算結果

#### 請求優化
- **請求合併**：合併多個 API 請求
- **請求取消**：取消不需要的請求
- **錯誤處理**：優雅的錯誤處理

### 2. 後端優化

#### 中間件優化
- **請求壓縮**：啟用 gzip 壓縮
- **快取標頭**：設置適當的快取標頭
- **CORS 優化**：最小化 CORS 配置

#### 資料庫優化
- **連線池**：使用連線池管理資料庫連線
- **查詢優化**：優化 SQL 查詢
- **索引策略**：適當的索引設計

### 3. 網路優化

#### CDN 使用
- **靜態資源**：使用 CDN 加速靜態資源
- **圖片優化**：壓縮和優化圖片
- **字體優化**：使用 Web 字體優化

#### 請求優化
- **HTTP/2**：使用 HTTP/2 協議
- **請求合併**：合併多個小請求
- **壓縮**：啟用內容壓縮

## 監控和警報

### 效能閾值

#### 響應時間警告
- **綠色**：< 500ms（良好）
- **黃色**：500ms - 1000ms（警告）
- **紅色**：> 1000ms（需要優化）

#### 錯誤率警告
- **綠色**：< 1%（良好）
- **黃色**：1% - 5%（警告）
- **紅色**：> 5%（需要修復）

### 自動優化

#### 快取自動清理
```typescript
// 每 5 分鐘清理過期快取
setInterval(() => {
  performanceMonitor.clearExpiredCache();
}, 300000);
```

#### 效能報告
```typescript
// 每 10 分鐘輸出效能摘要
setInterval(() => {
  performanceMonitor.logSummary();
}, 600000);
```

## 故障排除

### 常見問題

#### 1. 慢請求問題
- **檢查網路**：確認網路連線狀況
- **檢查 API**：確認 Google Maps API 狀態
- **檢查快取**：確認快取是否正常工作

#### 2. 記憶體洩漏
- **檢查快取**：確認快取大小是否過大
- **檢查監聽器**：確認事件監聽器是否正確清理
- **檢查請求**：確認請求是否正確取消

#### 3. 快取問題
- **檢查 TTL**：確認快取過期時間設置
- **檢查鍵值**：確認快取鍵值是否正確
- **檢查清理**：確認過期快取是否正確清理

### 除錯工具

#### 瀏覽器開發者工具
- **Network 面板**：查看請求詳情
- **Performance 面板**：分析效能瓶頸
- **Memory 面板**：檢查記憶體使用

#### 服務器日誌
- **請求日誌**：查看請求處理時間
- **錯誤日誌**：查看錯誤詳情
- **效能日誌**：查看效能指標

## 總結

通過實施這些效能優化策略，咖啡廳探索應用能夠：

- **提升響應速度**：平均響應時間 < 500ms
- **減少 API 請求**：通過快取和防抖動機制
- **優化用戶體驗**：減少載入時間和錯誤
- **節省資源**：降低服務器負載和頻寬使用
- **實時監控**：通過效能儀表板實時了解系統狀態

定期檢查效能指標，根據實際使用情況調整優化策略，確保應用始終保持最佳效能。
