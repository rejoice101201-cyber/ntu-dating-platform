{
  "buildCommand": "cd frontend && npm install && npx prisma generate && NEXT_PRIVATE_SKIP_BUILD_TRACE=1 npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && npm install",
  "framework": null,
  "rootDirectory": "frontend"
}

